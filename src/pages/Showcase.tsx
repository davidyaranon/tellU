import React from 'react';
import { RouteComponentProps } from "react-router-dom";

interface MatchUserPostParams {
  key: string;
}

const Showcase = ({ match }: RouteComponentProps<MatchUserPostParams>) => {
  return(
    null
  );
}

export default Showcase;
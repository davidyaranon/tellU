import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.quantum',
  appName: 'quantum-ionic',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
